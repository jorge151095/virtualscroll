import { BoundingScrollDirective } from './bounding-scroll.directive';

describe('BoundingScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new BoundingScrollDirective();
    expect(directive).toBeTruthy();
  });
});
